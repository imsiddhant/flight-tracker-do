kind: Service
apiVersion: v1
metadata:
  name: primary-lb
spec:
  type: LoadBalancer
  selector:
    app: flights-hyd
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000